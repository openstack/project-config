- job-template:
    name: '{pipeline}-kolla-functional-f21'
    node: '{node}'

    builders:
      - link-logs
      - net-info
      - gerrit-git-prep
      - shell: |
          #!/bin/bash -xe
          tests/setup_docker.sh
          tox -e images

    publishers:
      - console-log
