[access "refs/heads/*"]
abandon = group puppet-docker_registry-core
label-Code-Review = -2..+2 group puppet-docker_registry-core
label-Workflow = -1..+1 group puppet-docker_registry-core

[access "refs/tags/*"]
pushSignedTag = group puppet-docker_registry-release

[receive]
requireChangeId = true

[submit]
mergeContent = true
