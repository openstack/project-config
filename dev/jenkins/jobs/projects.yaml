#
# The projects in this file are sorted alphabetically.
#

- project:
    name: noop
    tarball-site: tarballs.openstack.org

    jobs:
      - '{pipeline}-noop':
          pipeline: gate
          node: ubuntu-xenial
